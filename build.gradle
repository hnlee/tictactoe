apply plugin: 'java'
apply plugin: 'application'

dependencies {
  testCompile 'junit:junit:4.12'
}

jar { 
    baseName = 'tictactoe'
    version = 0.1
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.14'
}
